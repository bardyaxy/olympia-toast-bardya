{
  "name": "olympia-toast-bardya",
  "version": "1.0.0",
  "description": "Simple build step for HTML includes",
  "scripts": {
    "build": "npm run build:css && node scripts/build.js",
    "serve": "node scripts/serve.js",
    "build:css": "sass styles/src/style.scss dist/style.min.css --no-source-map --style=compressed"
  },
  "devDependencies": {
    "sass": "^1.69.0"
  }
}
